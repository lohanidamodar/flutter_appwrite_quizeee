name: flutter_appwrite_quizeee
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  appwrite: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  dart_appwrite: null

flutter:
  uses-material-design: true
